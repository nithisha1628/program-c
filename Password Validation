#include <stdio.h>
#include <string.h>

int main() {
    char password[50];
    printf("Enter password: ");
    scanf("%s", password);

    if (strlen(password) >= 8)
        printf("Valid Password\n");
    else
        printf("Invalid Password (must be at least 8 characters)\n");

    return 0;
}
