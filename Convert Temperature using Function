#include <stdio.h>
float toCelsius(float f) { return (f-32)*5/9; }
float toFahrenheit(float c) { return (c*9/5)+32; }
int main() {
    float temp;
    printf("Enter temp in Fahrenheit: ");
    scanf("%f",&temp);
    printf("Celsius = %.2f\n", toCelsius(temp));
    return 0;
}
