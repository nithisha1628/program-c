#include <stdio.h>
struct Employee { int id; char name[30]; float salary; };
int main() {
    struct Employee e = {101,"Alice",50000};
    printf("ID:%d Name:%s Salary:%.2f\n", e.id, e.name, e.salary);
    return 0;
}
