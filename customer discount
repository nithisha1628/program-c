#include <stdio.h>
int main() {
    float amount;
    printf("Enter purchase amount: ");
    scanf("%f", &amount);

    if (amount >= 5000)
        printf("Discount Applied");
    else
        printf("No Discount");

    return 0;
}
